{
  "$schema": "https://deno.land/x/denon@2.4.7/schema.json",
  "scripts": {
    "start": {
      "cmd": "deno run -A --unstable --import-map=./import-map.json ./src/server.ts",
      "desc": "Run the moldy server "
    }
  }
}
